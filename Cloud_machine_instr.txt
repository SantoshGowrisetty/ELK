connecting to gcp machines:

use putty or ur linux machine to connect to gcp machines
from putty
--configure putty with provided hdu.ppk
  hdu@ipaddress
  pswd:welcome
from linx machine
chmod -R 400 hdu.pem
--ssh -i "hdu.pem" hdu@ipadress
--pswd:welcome

all machines have ssh password less access for root user
all machines have ssh password access for hdu user using pem file
